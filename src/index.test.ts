import BundleDeclarationsWebpackPlugin from ".";


describe(BundleDeclarationsWebpackPlugin.name, () => {
    test.todo("applies the default options");
    test.todo("supports overriding default options");
    test.todo("hooks processAssets for each compilation");
    test.todo("generates a .d.ts bundle for a single string entry-point");
    test.todo("generates a .d.ts bundle for multiple string entry-points");
    test.todo("generates a .d.ts bundle for a single EntryPointConfig");
    test.todo("generates a .d.ts bundle for an array of EntryPointConfigs");
    test.todo("emits the configured outFile as an asset");
});